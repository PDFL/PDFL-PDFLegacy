const SIMILARITY_THRESHOLD = 0.78;

const MAX_GRAPH_DEPTH = 2;
const MAX_CITATION = 8;
const MAX_REFERENCES = 8;

const TEN_MILLISECONDS = 10;

export { SIMILARITY_THRESHOLD, MAX_GRAPH_DEPTH, MAX_CITATION, MAX_REFERENCES, TEN_MILLISECONDS };
